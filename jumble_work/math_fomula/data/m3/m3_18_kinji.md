### $1$ 次の近似式

$|h|$ が十分小さいとき  

$$f(a + h) \fallingdotseq f(a) + f'(a)h$$
<br>

$|x|$ が十分小さいとき

$$f(x) \fallingdotseq f(0) + f'(0)x$$

とくに  

$$(1 + x)^{p} \fallingdotseq 1 + px$$

<br>


### 計算式の例

$$f(x) = (1 + x)^{5}$$
$$f(x) \fallingdotseq 1 + 5x$$
<br>

$$f(x) = \dfrac{1}{1 - x}$$
$$f'(x) = - \dfrac{- 1}{(1 - x)^{2}} = \dfrac{1}{(1 - x)^{2}}$$
$$f(x) \fallingdotseq 1 + x$$
<br>

$$f(x) = \log(1 + 2x)$$
$$f'(x) = \dfrac{2}{1 + 2x}$$
$$f(x) \fallingdotseq 2x$$
<br>

$$f(x) = \sin\left( \dfrac{\pi}{6} + x \right)$$
$$f'(x) = \cos\left( \dfrac{\pi}{6} + x \right)$$
$$f(x) \fallingdotseq \sin\dfrac{\pi}{6} + \cos\dfrac{\pi}{6} \cdot x = \dfrac{1}{2} + \dfrac{\sqrt{3}}{2}x$$
